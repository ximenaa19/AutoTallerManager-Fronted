import { MechanicRequestPartsGuidance } from '@/features/mechanic/components/MechanicRequestPartsGuidance';

/** Sidebar entry without order service context — guidance only. */
export function MechanicRequestPartsPage() {
  return <MechanicRequestPartsGuidance />;
}
